import styled from "styled-components";

const ProductCaseContainer = styled.div`
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
  max-width: 80%;
  margin-left: auto;
  margin-right: auto;
`

export { ProductCaseContainer }